'use strict'

// var Derived = require('./derived')

// console.log(Derived)

module.exports = class Base {
  constructor () {
    
  }

  create () {
    console.log('Derived')
    // console.log(Derived)
    // var derived = new Derived();
  }
}